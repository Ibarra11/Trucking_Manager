let initalState = {
    name: '',
    contactNumber: '',
    address: '',
    dateHired: '',
    unitNumber: ''
}

export default function(state=initalState, action){
    switch(action.type){
        default:{
            return state
        }
    }
}